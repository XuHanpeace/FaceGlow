// COS配置示例文件
// 复制此文件为 cosSecrets.js 并填入真实的配置信息
// 注意：cosSecrets.js 文件已添加到 .gitignore，不会被提交到版本控制

export const COS_SECRETS = {
  secretId: 'YOUR_SECRET_ID_HERE',  // 替换为您的真实 SecretId
  secretKey: 'YOUR_SECRET_KEY_HERE', // 替换为您的真实 SecretKey
};

// 导出默认配置
export default COS_SECRETS;
